/**
 * Entry Point for Project Settings Module
 * 
 * This file initializes the React application for the Project Settings interface.
 * It renders the ProjectSettings component into the root DOM element.
 * 
 * @module project-settings/index
 */

import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));