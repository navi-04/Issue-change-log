/**
 * Entry Point for Issue Change Log Module
 * 
 * This file initializes the React application for the main Issue Change Log interface.
 * It renders the App component (changelog viewer) into the root DOM element.
 * 
 * @module hello-world/index
 */

import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));
