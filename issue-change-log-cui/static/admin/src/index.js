/**
 * Entry Point for Admin Module
 * 
 * This file initializes the React application for the Admin interface.
 * It renders the AdminApp component into the root DOM element.
 * 
 * @module admin/index
 */

import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));